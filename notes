Goto github create repo

paste below

echo "# repoName" >> README.md check at github
git init
git add README.md

-- Interval --

git add .

-- Interval --

git commit -m "first commit"
git branch -M main
git remote add origin (repoLink) check at github
git push -u origin main

check at github

If reinitialize this is error delete .git hidden files in the codebase

https://www.simicart.com/manifest-generator.html/